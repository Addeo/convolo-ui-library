import { AlfaButtonDirective } from './alfa-button.directive';

describe('AlfaButtonDirective', () => {
    it('should create an instance', () => {
        const directive = new AlfaButtonDirective();
        expect(directive).toBeTruthy();
    });
});
